name: Desktop Build

on:
  push:
    branches: ['*']
  pull_request:
    branches: [master]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm run test:run

  build-electron:
    needs: test
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: ubuntu-22.04
            target: linux
            build_script: electron:build:linux
          - platform: windows-latest
            target: windows
            build_script: electron:build:win

    runs-on: ${{ matrix.platform }}

    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install Linux dependencies
        if: matrix.target == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y rpm

      - name: Install dependencies
        run: npm ci

      # Check if current commit on master has a version tag
      - name: Check for version tag
        id: check_tag
        shell: bash
        run: |
          TAG=$(git describe --tags --exact-match HEAD 2>/dev/null || echo "")
          if [[ "$TAG" == v* ]] && [[ "${{ github.ref }}" == "refs/heads/master" ]]; then
            echo "is_release=true" >> $GITHUB_OUTPUT
            echo "tag=$TAG" >> $GITHUB_OUTPUT
          else
            echo "is_release=false" >> $GITHUB_OUTPUT
          fi

      # Debug builds for branches (not master with tag)
      - name: Build Electron app (Debug)
        if: steps.check_tag.outputs.is_release != 'true'
        run: npm run ${{ matrix.build_script }} -- --publish never
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false

      # Release builds only on master with version tag
      - name: Build and Publish Electron app (Release)
        if: steps.check_tag.outputs.is_release == 'true'
        run: npm run ${{ matrix.build_script }} -- --publish always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: false

      - name: Upload Linux artifacts
        if: matrix.target == 'linux' && steps.check_tag.outputs.is_release != 'true'
        uses: actions/upload-artifact@v4
        with:
          name: linux-builds
          path: |
            release/*.deb
            release/*.rpm
            release/*.AppImage
          retention-days: 7

      - name: Upload Windows artifacts
        if: matrix.target == 'windows' && steps.check_tag.outputs.is_release != 'true'
        uses: actions/upload-artifact@v4
        with:
          name: windows-builds
          path: |
            release/*.exe
          retention-days: 7
